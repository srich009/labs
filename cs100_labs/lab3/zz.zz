currently editing unfinished

bubSort -> needs: sort(Container*)
selSort -> needs: sort(Container*)

listCont -> needs: sort(), swap()
vectCont -> needs: sort()

--------------------

base
{
    add
    sub
    mul
    div
    sqr
    op
}

sort
{
    bubSort
    selSort
}

container
{
    listCont
    vectCont
}