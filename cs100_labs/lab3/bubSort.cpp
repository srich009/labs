
// change to doubles

/*
void bubble_sort(vector<int> &v)
{
   for(unsigned i = 0; i < v.size(); i++)
   {
      for(unsigned j = i + 1; j < v.size(); j++)
      {
         if(v.at(i) > v.at(j))
         {
            swap(v.at(i), v.at(j));
         }
      }
   }
}
*/